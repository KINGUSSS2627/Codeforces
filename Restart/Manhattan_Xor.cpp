//                                        ॐ हनुमंते नमः
#include <bits/stdc++.h>
#define int long long int
using namespace std;
void solve()
{
    int n, m, x, y;
    cin >> n >> m >> x >> y;

    if (n < m)
    {
        swap(n, m);
        swap(x, y);
    }

    int x = 0;
}
int32_t main()
{
    int tc = 1;
    cin >> tc;
    while (tc--)
    {
        solve();
    }
    return 0;
}